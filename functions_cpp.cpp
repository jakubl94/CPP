#include "functions_cpp.h"
/*In this file, we declare define function which is then declared
in .h file also known as head file. Header file can be then included 
in which ever other .cpp file where we want to call that function.*/

double soucet(double a, double b)
{
    return a + b;
}