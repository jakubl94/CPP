
#pragma once
 /*This is a header file, it's used for 
function declarations and later on it's used
in files where we want to call the functions*/
double soucet(double a, double b);

/*To compile all the files use terminal:
Go to directory with all the files cd "Jakub", cd "C_Rrepo"
and then use clang++ file1.cpp file2.cpp filex.cpp.
Dont include header files. After sucesful compilation we
should get runnable .out file.*/